import React, { Fragment } from 'react';

import { View, Text } from 'react-native';

const CharacterInfo = () => {
  return (
    <View>
      <Text>Info</Text>
    </View>
  );
};

export default CharacterInfo;
